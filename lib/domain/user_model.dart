class UserModel {
  String ? email;
  String ? password;
  String ? uid;
  String ? name;
  String ? phone;
  String ? gender;
  String ?street;
  String ?city;
  String ?district;
  String ?image;


UserModel({this.email,this.password,this.uid,this.name,this.phone,this.gender, this.street, this.city,this.district,this.image});
}